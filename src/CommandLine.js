function CommandLine() {
    return (
        <div className="CommandLine" style={{position: "absolute", left: 0, right: 0, bottom: "1em"}}>
            <input type="text" style={{width: "90%"}} autoFocus></input>
        </div>
    );
}

export default CommandLine;
